<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Quotes</title>
  <link href="./style.css" rel="stylesheet" type="text/css">
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.0/css/all.css">
  <!-- Bootstrap core CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.2.1/css/bootstrap.min.css" rel="stylesheet">
  <!-- Material Design Bootstrap -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.7.3/css/mdb.min.css" rel="stylesheet">

    <style>
        @import url(https://fonts.googleapis.com/css?family=Roboto:500,700);

        *, *::before, *::after {
        box-sizing: border-box;
        }

        html {
        min-height: 100%;
        }

        body {
        margin: 20px;
        color: #435757;
        background: linear-gradient(-20deg, #d0b782 20%, #a0cecf 80%);
        font: 500 1.2em/1.2 'Roboto', sans-serif;
        }

        .container {
        max-width: 1000px;
        margin: 0 auto;
        border-top: 5px solid #435757;
        background-color: rgba(255, 255, 255, .2);
        box-shadow: 0 0 20px rgba(0, 0, 0, .1);
        user-select: none;
        }

        h1 {
        margin: 0;
        padding: 20px;
        background-color: rgba(255, 255, 255, .4);
        font-size: 1.8em;
        text-align: center;
        }

        .items {
        display: flex;
        flex-direction: column;
        padding: 20px;
        counter-reset: done-items undone-items;
        }


        /* hide inputs offscreen, but at the same vertical positions as the correpsonding labels, so that tabbing scrolls the viewport as expected */

        li {
        display: block;
        position: relative;
        padding: 15px 0 15px 45px;
        border-top: 1px dashed #fff;
        order: 4;
        cursor: pointer;
        animation: undone .5s;
        } */

        li::before {
        content: '\f10c'; /* circle outline */
        display: block;
        position: absolute;
        top: 11px;
        left: 10px;
        font: 1.5em 'FontAwesome';
        }

        li:hover, input:focus + li {
        background-color: rgba(255, 255, 255, .2);
        }

    </style>

</head>
<body>
<h2>Try Quotes</h2>
<br>

<form action="/quotes" method="POST" style="margin-left: 10%;">
    <div class="row">
        <!-- Material input Name-->
        <div class="md-form col-sm-3">
            <input type="text" id="form1" class="form-control" name="name">
            <label for="form1">Name</label>
        </div>

        <!-- Material input Quote-->
        <div class="md-form col-sm-6">
            <input type="text" id="form1" class="form-control" name="quote">
            <label for="form1">Quote</label>
        </div>

        <div class="md-form col-sm-2">
            <button type="submit" class="btn btn-primary btn-md mt-0">Submit</button>
        </div>
    </div>
</form>

<div class="container">

    <h1>Nik's Quoters and Quotes List</h1>
    <ul class="items">
        <% for(var i=0; i<quotes.length; i++) {%>
        <li>
            <span><q><i><%= quotes[i].quote %></i></q></span>
            <span class="text-right">   - <%= quotes[i].name %></span>
        </li>
        <% } %>
    </ul>         
</div>

  <!-- JQuery -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
<!-- Bootstrap tooltips -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.4/umd/popper.min.js"></script>
<!-- Bootstrap core JavaScript -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.2.1/js/bootstrap.min.js"></script>
<!-- MDB core JavaScript -->
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/mdbootstrap/4.7.3/js/mdb.min.js"></script>
</body>
</html>
